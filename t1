<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Eisenhower Matrix Todo</title>
  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="/_sdk/element_sdk.js"></script>
  <script src="/_sdk/data_sdk.js"></script>
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { font-family: 'Space Grotesk', sans-serif; }
    .paste-zone {
      border: 2px dashed currentColor;
      transition: all 0.3s ease;
    }
    .paste-zone:focus, .paste-zone.dragging {
      border-color: currentColor;
      background: rgba(99, 102, 241, 0.1);
    }
    .tab-active {
      border-bottom: 3px solid currentColor;
    }
    .task-item {
      animation: slideIn 0.3s ease-out;
    }
    @keyframes slideIn {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .chart-bar {
      transition: height 0.5s ease-out, background 0.3s ease;
    }
    .priority-badge {
      font-size: 0.65rem;
      padding: 2px 6px;
      border-radius: 4px;
    }
  </style>
</head>
<body class="h-full bg-slate-900 text-slate-100">
  <div id="app-container" class="h-full w-full flex flex-col overflow-auto">
    <!-- Header -->
    <header class="px-6 py-4 bg-slate-800 border-b border-slate-700">
      <h1 id="app-title" class="text-2xl font-bold text-indigo-400">Eisenhower Matrix Todo</h1>
      <p class="text-slate-400 text-sm mt-1">Prioritize tasks using the Eisenhower Matrix</p>
    </header>

    <!-- Tabs -->
    <nav class="flex bg-slate-800 border-b border-slate-700">
      <button id="tab-tasks" class="px-6 py-3 text-indigo-400 tab-active font-medium transition-colors" onclick="switchTab('tasks')">
        📝 Tasks
      </button>
      <button id="tab-chart" class="px-6 py-3 text-slate-400 font-medium hover:text-slate-200 transition-colors" onclick="switchTab('chart')">
        📊 Matrix View
      </button>
    </nav>

    <!-- Main Content -->
    <main class="flex-1 p-6 overflow-auto">
      <!-- Tasks Tab -->
      <div id="tasks-view">
        <!-- Input Section -->
        <div class="mb-6">
          <div class="flex gap-3 mb-4">
            <input 
              type="text" 
              id="task-input" 
              placeholder="Add a new task..."
              class="flex-1 px-4 py-3 bg-slate-800 border border-slate-600 rounded-lg focus:outline-none focus:border-indigo-500 text-slate-100 placeholder-slate-500"
              onkeypress="if(event.key === 'Enter') addTask()"
            >
            <select id="priority-select" class="px-4 py-3 bg-slate-800 border border-slate-600 rounded-lg focus:outline-none focus:border-indigo-500 text-slate-100">
              <option value="urgent-important">🔴 Urgent & Important</option>
              <option value="not-urgent-important">🟡 Not Urgent & Important</option>
              <option value="urgent-not-important">🟠 Urgent & Not Important</option>
              <option value="not-urgent-not-important">🟢 Not Urgent & No
